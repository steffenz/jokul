import React from "react";
import { Layout } from "../components";

const NotFoundPage = () => (
    <Layout>
        <h1 className="jkl-title-large jkl-spacing--top3">NOT FOUND</h1>
        <p className="jkl-lead jkl-spacing--top-1">You just hit a route that doesn&#39;t exist... the sadness.</p>
    </Layout>
);

export default NotFoundPage;
